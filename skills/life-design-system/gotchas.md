# 常见陷阱

此文件用于记录 `life-design-system` 在实现页面和维护文档时的高频问题。

当前建议优先沉淀以下内容：

- 列表页筛选区必须使用 `FilterGroup` 体系，且子项需要声明 `name`
- 表格表头与内容的 `align` 必须一致并显式声明
- 详情页展示态优先使用 `KeyValue`，不要使用禁用态表单控件伪装
- 图标、token、skills 修改后需要执行对应同步脚本
- 修改 skill 结构后，文档内的相对路径引用需要一起更新
